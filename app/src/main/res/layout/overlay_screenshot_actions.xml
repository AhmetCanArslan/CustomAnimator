<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/overlay_copy"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_screenshot_overlay_button"
        android:clickable="true"
        android:focusable="false"
        android:contentDescription="@string/screenshot_overlay_copy_desc"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_overlay_copy"
        android:tint="#FFFFFFFF" />

    <ImageView
        android:id="@+id/overlay_delete"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/bg_screenshot_overlay_button"
        android:clickable="true"
        android:focusable="false"
        android:contentDescription="@string/screenshot_overlay_delete_desc"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_overlay_delete"
        android:tint="#FFFFFFFF" />
</LinearLayout>
